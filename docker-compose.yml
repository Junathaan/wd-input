version: "2.3"

services:
  front:
    container_name: front
    hostname: front
    build:
      context: ./front
      dockerfile: node.dockerfile
    volumes:
      - ./front:/usr/front
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    command: sh -c "npm install && npm run serve"